# Handle optional dependencies properly
optional-dependencies=false
# Ensure platform-specific binaries are installed
platform=linux
arch=x64
# Use npm's legacy peer deps to avoid conflicts
legacy-peer-deps=true 